class LL{
      Consumer*hol;
      Consumer*tail;
      int size;
public:
       void add_Consumer(Consumer*&);
       void show_all();
       void delete_all();
       void call();
       ~LL();
       LL();
      
};
